# Projarium

Safe GitHub package for the Projarium PHP application.

## Configuration

Set the environment variables in `.env.example`. Production MySQL credentials and the production initial admin password were removed. To seed the first admin account, set `PROJARIUM_INITIAL_ADMIN_PASSWORD` before the first run.
